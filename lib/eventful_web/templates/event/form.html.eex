<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :sender_info %>
  <%= textarea f, :sender_info %>
  <%= error_tag f, :sender_info %>

  <%= label f, :payload %>
  <%= textarea f, :payload %>
  <%= error_tag f, :payload %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
